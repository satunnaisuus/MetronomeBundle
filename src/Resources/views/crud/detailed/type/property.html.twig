{% set value = attribute(entity, element.options.property) %}

{% if value is same as(null) %}
    {{ include('@Metronome/crud/field/null.html.twig') }}
{% else %}
    {{ include('@Metronome/crud/field/' ~ element.options.inner_type.type ~ '.html.twig', {value: value }) }}
{% endif %}