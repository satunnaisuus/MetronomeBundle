{% set value = attribute(entity, element.options.property) %}

{% if value is same as(null) %}
    {{ include('@Admin/crud/field/null.html.twig') }}
{% else %}
    {{ include('@Admin/crud/field/' ~ element.options.inner_type.type ~ '.html.twig', {value: value }) }}
{% endif %}